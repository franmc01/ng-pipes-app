import { HeroTextColorPipe } from './hero-text-color.pipe';

describe('HeroTextColorPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroTextColorPipe();
    expect(pipe).toBeTruthy();
  });
});
